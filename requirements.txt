streamlit>=1.39.0
langchain==0.3.0
langchain-google-genai==2.0.0
langchain-openai==0.2.1
openai==1.52.2
google-generativeai==0.7.2
PyMuPDF==1.24.11
pillow>=11.0.0
# 注意：pyarrow 是 streamlit 的可选依赖，已从 requirements.txt 中移除
# 如果 Python 3.13 没有预编译 wheel，streamlit 会在没有 pyarrow 的情况下运行
# 这不会影响 PDF 处理功能，只会影响某些数据可视化功能
python-dotenv==1.0.1
tqdm==4.66.5
markdown==3.6
beautifulsoup4==4.12.3
# 可选：更好的 Markdown 支持（表格/代码高亮等），如不需要可移除
pymdown-extensions==10.8.1
# 可选：Windows 字体检测（可选安装，如果未安装将使用默认字体列表）
# pywin32>=306
